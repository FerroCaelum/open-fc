// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Note {
  id         String   @id @default(uuid()) @db.Uuid
  text       String
  title      String
  lastUpdate DateTime @updatedAt
  created    DateTime @default(now())
}

model GameEntity {
  id                  String           @id @default(uuid()) @db.Uuid
  name                String           @unique
  text                String
  tags                String[]
  lastUpdate          DateTime         @updatedAt
  created             DateTime         @default(now())
  gameEntityLinksFrom GameEntityLink[]
}

model GameEntityLink {
  id          String     @id @default(uuid()) @db.Uuid
  from        GameEntity @relation(fields: [fromId], references: [id])
  fromId      String
  to          GameEntity @relation(fields: [toId], references: [id])
  toId        String
  description String
  created     DateTime   @default(now())
}
